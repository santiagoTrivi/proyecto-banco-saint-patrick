import { tv } from 'tailwind-variants';

export const InputStyles = tv({
	base: 'border border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm text-bg1 px-2 py-1 text-md'
});
